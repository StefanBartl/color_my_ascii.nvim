1. `doc/asci_my_color.txt` is not wired up yet. The plugin's autodoc is not
   picked up: typing `:h colo_my_asci` should bring up that file. For that it
   has to be registered on the plugin's first initialization in nvim.
2. `doc/asci_my_color.txt` also still needs updating.
3. Point out the alpha status in the README and add tags.
